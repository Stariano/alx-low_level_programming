#include "main.h"
#include <string.h>

/**
 * _strlen - unction that returns the length of a string
 * @s: The string.
 * Return: Strlen of the word/
 */

int _strlen(char *s)
{
	return (strlen(s));
}
