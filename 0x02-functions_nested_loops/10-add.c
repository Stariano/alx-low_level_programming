#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @first: first number input
 * @second: second number input
 * Return: first + second
 */
int add(int first, int second)
{
return (first + second);
}
