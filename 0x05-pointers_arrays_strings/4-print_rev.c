#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_rev - function that prints a string, in reverse, followed by a new line.
 * @s: The string.
 */
 
void print_rev(char *s)
{
	printf("%s", revstr(s));
}
