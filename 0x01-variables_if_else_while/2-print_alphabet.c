#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Assign a random variable with conditional if and get the last digit*
 *Return: Always 0 (success)
 */

int main(void)
{
int x;
for (x = 'a'; x <= 'z'; x++)
putchar(x);

putchar('\n');
return (0);
}
