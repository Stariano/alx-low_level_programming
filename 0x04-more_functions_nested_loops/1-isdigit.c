#include "main.h"
#include <ctype.h>

/**
 * _isupper - checks for uppercase character
 * @c: int to receive data
 * Return: 1 / 0
 */
int _isdigit(int c)
{
int a = isdigit(c);

if (a == 0)
return (a);
else
return (1);
}

